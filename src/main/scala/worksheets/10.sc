
val list = List(1, 2, 3)

list: List[Int]

list(1)
list.apply(1)

list: Int ⇒ Int

val set = Set(1, 2, 3, 1)

set: Set[Int]

set.contains(30)
set(30)
set.apply(30)

set: Int ⇒ Boolean

val map = Map(1 → "a", 2 → "b")
map: Map[Int, String]

map(1)
map.apply(2)

map: Int ⇒ String

